{
  "name": "openclearance-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The openclearance.org standards site for the Clearance Manifest open standard.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "conformance": "node scripts/conformance.mjs",
    "deploy": "astro build && wrangler pages deploy dist --project-name=openclearance --branch=main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.13.0",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "jsonld": "^9.0.0"
  }
}
